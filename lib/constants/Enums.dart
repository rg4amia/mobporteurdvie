
enum Vp2Character { un, deux, trois, quatre }
enum Vp1Character { un, deux, trois, quatre }
enum Vp3Character { un, deux, trois, quatre }
enum Vp4Character { un, deux, trois, quatre }
enum M2Character { un, deux, trois, quatre }
enum M1Character { un, deux, trois, quatre }
enum M3Character { un, deux, trois, quatre }
enum PlanCharacter { un, deux, trois }
enum Exo1Character { un, deux, trois, quatre }
enum Exo2Character { un, deux, trois, quatre }
enum Exo3Character { un, deux, trois, quatre }
enum Exo4Character { un, deux, trois, quatre }
enum Exo5Character { un, deux, trois, quatre }
enum Exo6Character { un, deux, trois, quatre }
enum Temoin3Character { un, deux, trois, quatre, cinq, six, sept, huit, neuf}